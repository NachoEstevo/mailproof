# Secrets and environment
.env
.env.*
!.env.example
*.pem
*.key

# Midnight local state — .midnight-state.json holds wallet seeds and the
# 24-word recovery phrase. Never commit it.
.midnight-state.json
.midnight-wallet-state/
deployment.json

# Private-state store written by levelPrivateStateProvider. Holds the
# subject secret, which is the whole point of the subject binding.
midnight-level-db/
*-level-db/

# Private email fixtures — never commit a real .eml.
# Synthetic demo fixtures under fixtures/emails/ are allowed (see §75).
fixtures/private-emails/
*.eml
!fixtures/emails/*.eml

# Dependencies and build output
node_modules/
dist/
build/
managed/
.next/
coverage/

# Tooling caches
.turbo/
*.tsbuildinfo
.vite/
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Dedicated browser profile for driving the ZK Email registry flow.
# Holds real login sessions. Never commit.
.browser-profile/
