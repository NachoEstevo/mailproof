{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'no dependencies: the landing is static'",
  "buildCommand": "rm -rf dist && cp -R apps/landing dist",
  "outputDirectory": "dist"
}
